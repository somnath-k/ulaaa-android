<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme applied before Compose draws. Compose owns actual colors via UlaaaTheme. -->
    <style name="Theme.Ulaaa" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
